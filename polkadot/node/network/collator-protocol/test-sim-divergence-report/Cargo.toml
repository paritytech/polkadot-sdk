[package]
name = "polkadot-collator-protocol-test-sim-divergence-report"
version = "0.1.0"
description = "Tooling: summarise the test-sim differential suite's open-bug inventory and current regression status."
authors.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
syn = { workspace = true, features = ["full", "parsing"] }
walkdir = { workspace = true }

[[bin]]
name = "divergence-report"
path = "src/main.rs"
