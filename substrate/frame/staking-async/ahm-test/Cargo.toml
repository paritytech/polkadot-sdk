[package]
name = "pallet-ahm-test"
version = "1.0.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
homepage.workspace = true
repository.workspace = true
description = "e2e unit tests for staking in AHM"
publish = false

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dev-dependencies]
codec = { features = ["derive"], workspace = true, default-features = true }
frame = { default-features = true, workspace = true }
frame-support = { default-features = true, workspace = true }
log = { workspace = true }
scale-info = { features = ["derive"], workspace = true, default-features = true }
sp-core = { default-features = true, workspace = true }
sp-session = { default-features = true, workspace = true }
sp-staking = { default-features = true, workspace = true }
sp-tracing = { default-features = true, workspace = true }

# pallets we need in both
pallet-balances = { default-features = true, workspace = true }

# pallets that we need in AH
frame-election-provider-support = { default-features = true, workspace = true }
pallet-election-provider-multi-block = { default-features = true, workspace = true }
pallet-staking-async = { default-features = true, workspace = true }
pallet-staking-async-rc-client = { default-features = true, workspace = true }

# pallets we need in the RC
pallet-authorship = { default-features = true, workspace = true }
pallet-session = { default-features = true, workspace = true }
pallet-staking-async-ah-client = { default-features = true, workspace = true }
pallet-timestamp = { default-features = true, workspace = true }
# staking classic which will be replaced by ah-client
pallet-root-offences = { default-features = true, workspace = true }
pallet-staking = { default-features = true, workspace = true }

[features]
std = [
	"log/std",
]
try-runtime = [
	"frame-election-provider-support/try-runtime",
	"frame-support/try-runtime",
	"frame/try-runtime",
	"pallet-authorship/try-runtime",
	"pallet-balances/try-runtime",
	"pallet-election-provider-multi-block/try-runtime",
	"pallet-root-offences/try-runtime",
	"pallet-session/try-runtime",
	"pallet-staking-async-ah-client/try-runtime",
	"pallet-staking-async-rc-client/try-runtime",
	"pallet-staking-async/try-runtime",
	"pallet-staking/try-runtime",
	"pallet-timestamp/try-runtime",
]
