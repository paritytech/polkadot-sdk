[package]
name = "pallet-staking-async-rc-client"
description = "Pallet handling the communication with staking-ah-client. It's role is to glue the staking pallet (on AssetHub chain) and session pallet (on Relay Chain) in a transparent way."
license = "Apache-2.0"
version = "0.7.0"
edition.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
codec = { workspace = true, features = ["derive"] }
<<<<<<< HEAD
frame-support.workspace = true
frame-system.workspace = true
=======
frame-benchmarking = { optional = true, workspace = true }
frame-support = { workspace = true }
frame-system = { workspace = true }
>>>>>>> f154a346 (staking-async: allow  session keys handling on AssetHub (#10666))
impl-trait-for-tuples = "0.2.2"
log = { workspace = true }
scale-info = { workspace = true, features = ["derive"] }
sp-core.workspace = true
sp-runtime = { features = ["serde"], workspace = true }
sp-staking = { features = ["serde"], workspace = true }
<<<<<<< HEAD
xcm.workspace = true
=======
# XCM dependencies are only needed for the optional XCMSender helper struct.
# They can be removed entirely once XCMSender is moved to a separate crate.
xcm = { optional = true, workspace = true }
xcm-builder = { optional = true, workspace = true }
xcm-executor = { optional = true, workspace = true }
>>>>>>> f154a346 (staking-async: allow  session keys handling on AssetHub (#10666))

[features]
default = ["std"]
std = [
	"codec/std",
	"frame-benchmarking?/std",
	"frame-support/std",
	"frame-system/std",
	"log/std",
	"scale-info/std",
	"sp-core/std",
	"sp-runtime/std",
	"sp-staking/std",
	"xcm-builder?/std",
	"xcm-executor?/std",
	"xcm?/std",
]
# Enables the XCMSender helper struct for runtimes that want to use XCM transport.
xcm-sender = ["xcm"]
runtime-benchmarks = [
	"frame-benchmarking/runtime-benchmarks",
	"frame-support/runtime-benchmarks",
	"frame-system/runtime-benchmarks",
	"sp-runtime/runtime-benchmarks",
	"sp-staking/runtime-benchmarks",
	# Benchmarks require XCM for delivery fee setup
	"xcm",
	"xcm-builder",
	"xcm-builder/runtime-benchmarks",
	"xcm-executor",
	"xcm-executor/runtime-benchmarks",
	"xcm/runtime-benchmarks",
]
try-runtime = [
	"frame-support/try-runtime",
	"frame-system/try-runtime",
	"sp-runtime/try-runtime",
]
