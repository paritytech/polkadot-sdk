[build]
target="wasm32-unknown-unknown"

# see cargo-contract/crates/build/src/args.rs
rustflags = [
	"-C", "link-arg=-zstack-size=65536",
	"-C", "link-arg=--import-memory",
	"-C", "target-cpu=mvp",
]

# does not seem to work for now...
# fails with duplicate lang item in crate `core` (which `pallet_contracts_uapi` depends on):
[unstable]
build-std=["core,alloc"]
build-std-features=["panic_immediate_abort"]
