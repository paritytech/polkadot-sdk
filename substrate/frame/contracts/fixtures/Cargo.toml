[package]
name = "pallet-contracts-fixtures"
publish = false
version = "0.1.0"
authors.workspace = true
edition.workspace = true
license.workspace = true
description = "Fixtures for testing contracts pallet."

[dependencies]
wat = "1"
frame-system = { path = "../../system", default-features = false, version = "26.0.0" }
sp-runtime = { path = "../../../primitives/runtime", default-features = false, version = "29.0.0" }

[features]
default = [ "std" ]
std = [ "frame-system/std", "sp-runtime/std" ]

