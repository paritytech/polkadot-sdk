[package]
name = "frame"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io>"]
edition = "2021"
license = "Apache-2.0"
homepage = "paritytech.github.io"
repository.workspace = true
description = "The single package to get you started with building frame pallets and runtimes"
publish = false

[lints]
workspace = true

[package.metadata.docs.rs]
# enable `experimental` feature for docs
features = ["experimental"]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
# external deps
parity-scale-codec = { version = "3.2.2", default-features = false, features = ["derive"] }
scale-info = { version = "2.6.0", default-features = false, features = ["derive"] }

# primitive deps, used for developing FRAME pallets.
sp-runtime = { default-features = false, path = "../primitives/runtime", version = "32.0.0" }
sp-std = { default-features = false, path = "../primitives/std", version = "14.0.0" }
sp-io = { default-features = false, path = "../primitives/io", version = "31.0.0" }
sp-core = { default-features = false, path = "../primitives/core", version = "29.0.0" }
sp-arithmetic = { default-features = false, path = "../primitives/arithmetic", version = "24.0.0" }

# frame deps, for developing FRAME pallets.
frame-support = { default-features = false, path = "support", version = "29.0.2" }
frame-system = { default-features = false, path = "system", version = "29.0.0" }

# primitive types used for developing FRAME runtimes.
sp-version = { default-features = false, path = "../primitives/version", optional = true, version = "30.0.0" }
sp-api = { default-features = false, path = "../primitives/api", optional = true, version = "27.0.0" }
sp-block-builder = { default-features = false, path = "../primitives/block-builder", optional = true, version = "27.0.0" }
sp-transaction-pool = { default-features = false, path = "../primitives/transaction-pool", optional = true, version = "27.0.0" }
sp-offchain = { default-features = false, path = "../primitives/offchain", optional = true, version = "27.0.0" }
sp-session = { default-features = false, path = "../primitives/session", optional = true, version = "28.0.0" }
sp-consensus-aura = { default-features = false, path = "../primitives/consensus/aura", optional = true, version = "0.33.0" }
sp-consensus-grandpa = { default-features = false, path = "../primitives/consensus/grandpa", optional = true, version = "14.0.0" }
sp-inherents = { default-features = false, path = "../primitives/inherents", optional = true, version = "27.0.0" }

frame-executive = { default-features = false, path = "executive", optional = true, version = "29.0.0" }
frame-system-rpc-runtime-api = { default-features = false, path = "system/rpc/runtime-api", optional = true, version = "27.0.0" }

docify = "0.2.7"
simple-mermaid = { version = "0.1.1", optional = true }
log = { version = "0.4.20", default-features = false }

[dev-dependencies]
pallet-examples = { path = "examples" }

[features]
default = ["runtime", "std"]
experimental = ["frame-support/experimental"]
runtime = [
	"frame-executive",
	"frame-system-rpc-runtime-api",
	"sp-api",
	"sp-block-builder",
	"sp-consensus-aura",
	"sp-consensus-grandpa",
	"sp-inherents",
	"sp-offchain",
	"sp-session",
	"sp-transaction-pool",
	"sp-version",
]
std = [
	"frame-executive?/std",
	"frame-support/std",
	"frame-system-rpc-runtime-api?/std",
	"frame-system/std",
	"log/std",
	"parity-scale-codec/std",
	"scale-info/std",
	"simple-mermaid",
	"sp-api?/std",
	"sp-arithmetic/std",
	"sp-block-builder?/std",
	"sp-consensus-aura?/std",
	"sp-consensus-grandpa?/std",
	"sp-core/std",
	"sp-inherents?/std",
	"sp-io/std",
	"sp-offchain?/std",
	"sp-runtime/std",
	"sp-session?/std",
	"sp-std/std",
	"sp-transaction-pool?/std",
	"sp-version?/std",
]
