[package]
name = "frame-support-test-compile-pass"
version = "0.0.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
publish = false
homepage = "https://substrate.io"
repository.workspace = true
description = "frame-support-test-compile-pass"

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { package = "parity-scale-codec", version = "3.6.1", default-features = false, features = ["derive"] }
scale-info = { version = "2.5.0", default-features = false, features = ["derive"] }
renamed-frame-support = { package = "frame-support", path = "../..", default-features = false, version = "23.0.0" }
frame-system = { path = "../../../system", default-features = false, version = "23.0.0" }
sp-core = { path = "../../../../primitives/core", default-features = false, version = "23.0.0" }
sp-runtime = { path = "../../../../primitives/runtime", default-features = false, version = "26.0.0" }
sp-version = { path = "../../../../primitives/version", default-features = false, version = "24.0.0" }

[features]
default = [ "std" ]
std = [
	"codec/std",
	"frame-system/std",
	"renamed-frame-support/std",
	"scale-info/std",
	"sp-core/std",
	"sp-runtime/std",
	"sp-version/std",
]
