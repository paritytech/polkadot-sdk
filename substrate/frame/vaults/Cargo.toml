[package]
name = "pallet-vaults"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
homepage.workspace = true
repository.workspace = true
description = "FRAME pallet for the Liquity-V2-style pUSD vaults engine."

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { workspace = true, features = ["derive"] }
frame = { workspace = true, features = ["runtime"] }
log = { workspace = true }
pallet-linked-list = { workspace = true }
pusd-primitives = { workspace = true }
scale-info = { workspace = true, features = ["derive"] }

[dev-dependencies]
pallet-assets = { workspace = true, features = ["std"] }
pallet-assets-holder = { workspace = true, features = ["std"] }
pallet-balances = { workspace = true, features = ["std"] }
pallet-timestamp = { workspace = true, features = ["std"] }

[features]
default = ["std"]
std = [
	"codec/std",
	"frame/std",
	"log/std",
	"pallet-assets-holder/std",
	"pallet-assets/std",
	"pallet-balances/std",
	"pallet-linked-list/std",
	"pallet-timestamp/std",
	"pusd-primitives/std",
	"scale-info/std",
]
runtime-benchmarks = [
	"frame/runtime-benchmarks",
	"pallet-assets-holder/runtime-benchmarks",
	"pallet-assets/runtime-benchmarks",
	"pallet-balances/runtime-benchmarks",
	"pallet-linked-list/runtime-benchmarks",
	"pallet-timestamp/runtime-benchmarks",
	"pusd-primitives/runtime-benchmarks",
]
try-runtime = [
	"frame/try-runtime",
	"pallet-assets-holder/try-runtime",
	"pallet-assets/try-runtime",
	"pallet-balances/try-runtime",
	"pallet-linked-list/try-runtime",
	"pallet-timestamp/try-runtime",
	"pusd-primitives/try-runtime",
]
