[package]
name = "frame-executive"
version = "29.0.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
homepage = "https://substrate.io"
repository.workspace = true
description = "FRAME executives engine"
readme = "README.md"

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { package = "parity-scale-codec", version = "3.6.1", default-features = false, features = [
	"derive",
] }
log = { version = "0.4.17", default-features = false }
scale-info = { version = "2.10.0", default-features = false, features = ["derive"] }
frame-support = { path = "../support", default-features = false, version = "29.0.2" }
frame-system = { path = "../system", default-features = false, version = "29.0.0" }
frame-try-runtime = { path = "../try-runtime", default-features = false, optional = true, version = "0.35.0" }
sp-core = { path = "../../primitives/core", default-features = false, version = "29.0.0" }
sp-io = { path = "../../primitives/io", default-features = false, version = "31.0.0" }
sp-runtime = { path = "../../primitives/runtime", default-features = false, version = "32.0.0" }
sp-std = { path = "../../primitives/std", default-features = false, version = "14.0.0" }
sp-tracing = { path = "../../primitives/tracing", default-features = false, version = "16.0.0" }

[dev-dependencies]
array-bytes = "6.1"
pallet-balances = { path = "../balances" }
pallet-transaction-payment = { path = "../transaction-payment" }
sp-core = { path = "../../primitives/core" }
sp-inherents = { path = "../../primitives/inherents" }
sp-io = { path = "../../primitives/io" }
sp-version = { path = "../../primitives/version" }

[features]
default = ["std"]
with-tracing = ["sp-tracing/with-tracing"]
std = [
	"codec/std",
	"frame-support/std",
	"frame-system/std",
	"frame-try-runtime/std",
	"log/std",
	"scale-info/std",
	"sp-core/std",
	"sp-io/std",
	"sp-runtime/std",
	"sp-std/std",
	"sp-tracing/std",
]
try-runtime = [
	"frame-support/try-runtime",
	"frame-system/try-runtime",
	"frame-try-runtime/try-runtime",
	"sp-runtime/try-runtime",
]
