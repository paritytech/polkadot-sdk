[package]
name = "frame-election-solution-type-fuzzer"
version = "2.0.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
homepage.workspace = true
repository.workspace = true
description = "Fuzzer for phragmén solution type implementation."
publish = false

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[[bin]]
name = "compact"
path = "src/compact.rs"

[dependencies]
honggfuzz = { workspace = true }

codec = { features = ["derive"], workspace = true }
frame-election-provider-solution-type = { default-features = true, workspace = true }
frame-election-provider-support = { default-features = true, workspace = true }
frame-support = { default-features = true, workspace = true }
sp-arithmetic = { default-features = true, workspace = true }
sp-runtime = { default-features = true, workspace = true }
