[package]
name = "sp-session"
version = "22.0.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
homepage = "https://substrate.io"
repository.workspace = true
description = "Primitives for sessions"
readme = "README.md"

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { package = "parity-scale-codec", version = "3.6.1", default-features = false, features = ["derive"] }
scale-info = { version = "2.5.0", default-features = false, features = ["derive"] }
sp-api = { path = "../api", default-features = false, version = "21.0.0" }
sp-core = { path = "../core", default-features = false, version = "23.0.0" }
sp-runtime = { path = "../runtime", optional = true, version = "26.0.0" }
sp-staking = { path = "../staking", default-features = false, version = "21.0.0" }
sp-std = { path = "../std", default-features = false, version = "10.0.0" }
sp-keystore = { path = "../keystore", optional = true, version = "0.29.0" }

[features]
default = [ "std" ]
std = [
	"codec/std",
	"scale-info/std",
	"sp-api/std",
	"sp-core/std",
	"sp-keystore/std",
	"sp-runtime/std",
	"sp-staking/std",
	"sp-std/std",
]
