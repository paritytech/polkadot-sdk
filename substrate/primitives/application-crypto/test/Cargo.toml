[package]
name = "sp-application-crypto-test"
version = "0.0.0"
authors.workspace = true
edition.workspace = true
description = "Integration tests for application-crypto"
license = "Apache-2.0"
publish = false
homepage = "https://substrate.io"
repository.workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
sp-api = { version = "21.0.0", path = "../../api" }
sp-application-crypto = { version = "25.0.0", path = ".." }
sp-core = { path = "../../core", default-features = false, version = "23.0.0" }
sp-keystore = { path = "../../keystore", default-features = false, version = "0.29.0" }
substrate-test-runtime-client = { version = "0.0.0", path = "../../../test-utils/runtime/client" }
