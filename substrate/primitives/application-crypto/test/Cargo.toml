[package]
name = "sp-application-crypto-test"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
description = "Integration tests for application-crypto (polkadot v1.12.0)"
license = "Apache-2.0"
publish = false
homepage = "https://substrate.io"
repository.workspace = true

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
sp-api = { version = "32.0.0", path = "../../api" }
sp-application-crypto = { version = "36.0.0", path = ".." }
sp-core = { path = "../../core", default-features = false, version = "33.0.1" }
sp-keystore = { path = "../../keystore", default-features = false, version = "0.39.0" }
substrate-test-runtime-client = { version = "0.1.0", path = "../../../test-utils/runtime/client" }
