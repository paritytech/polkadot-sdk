[package]
name = "sp-authority-discovery"
version = "33.0.0"
authors.workspace = true
description = "Authority discovery primitives (polkadot v1.13.0)"
edition.workspace = true
license = "Apache-2.0"
homepage = "https://substrate.io"
repository.workspace = true
readme = "README.md"

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { package = "parity-scale-codec", version = "3.6.12", default-features = false }
scale-info = { version = "2.11.1", default-features = false, features = ["derive"] }
sp-api = { path = "../api", default-features = false, version = "33.0.0" }
sp-application-crypto = { path = "../application-crypto", default-features = false, version = "37.0.0" }
sp-runtime = { path = "../runtime", default-features = false, version = "38.0.0" }

[features]
default = ["std"]
std = [
	"codec/std",
	"scale-info/std",
	"sp-api/std",
	"sp-application-crypto/std",
	"sp-runtime/std",
]
serde = [
	"scale-info/serde",
	"sp-application-crypto/serde",
	"sp-runtime/serde",
]
