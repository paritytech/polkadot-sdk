{# Placeholder replaced by build-changelogs.sh with the changelog generated by
   scripts/release/changelog/generate.py. The substitution happens AFTER tera has
   rendered the document, so prdoc content is never parsed as a tera template
   (descriptions may legitimately contain `{{` or `{%`, e.g. Rust backtraces). #}
<!-- POLKADOT-SDK-CHANGELOG-BODY -->
