[package]
name = "cumulus-primitives-proof-size-hostfunction"
version = "0.9.0"
authors.workspace = true
edition.workspace = true
description = "Hostfunction exposing storage proof size to the runtime. (polkadot v1.13.0)"
license = "Apache-2.0"

[lints]
workspace = true

[dependencies]
sp-runtime-interface = { path = "../../../substrate/primitives/runtime-interface", default-features = false, version = "28.0.0" }
sp-externalities = { path = "../../../substrate/primitives/externalities", default-features = false, version = "0.29.0" }
sp-trie = { path = "../../../substrate/primitives/trie", default-features = false, version = "36.0.0" }

[dev-dependencies]
sp-state-machine = { path = "../../../substrate/primitives/state-machine" }
sp-core = { path = "../../../substrate/primitives/core" }
sp-io = { path = "../../../substrate/primitives/io" }

[features]
default = ["std"]
std = ["sp-externalities/std", "sp-runtime-interface/std", "sp-trie/std"]
