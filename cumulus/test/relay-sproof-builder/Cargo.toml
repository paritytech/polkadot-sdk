[package]
name = "cumulus-test-relay-sproof-builder"
version = "0.14.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
description = "Mocked relay state proof builder for testing Cumulus. (polkadot v1.13.0)"

[lints]
workspace = true

[dependencies]
codec = { package = "parity-scale-codec", version = "3.6.12", default-features = false, features = ["derive"] }

# Substrate
sp-runtime = { path = "../../../substrate/primitives/runtime", default-features = false, version = "38.0.0" }
sp-state-machine = { path = "../../../substrate/primitives/state-machine", default-features = false, version = "0.42.0" }
sp-trie = { path = "../../../substrate/primitives/trie", default-features = false, version = "36.0.0" }
sp-std = { path = "../../../substrate/primitives/std", default-features = false, version = "14.0.0" }

# Polkadot
polkadot-primitives = { path = "../../../polkadot/primitives", default-features = false, version = "14.0.0" }

# Cumulus
cumulus-primitives-core = { path = "../../primitives/core", default-features = false, version = "0.14.0" }

[features]
default = ["std"]
std = [
	"codec/std",
	"cumulus-primitives-core/std",
	"polkadot-primitives/std",
	"sp-runtime/std",
	"sp-state-machine/std",
	"sp-std/std",
	"sp-trie/std",
]
