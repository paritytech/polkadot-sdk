[relaychain]
default_command = "./target/release/polkadot"
default_args = [ "-lparachain=debug" ]
chain = "rococo-local"

  [[relaychain.nodes]]
  name = "alice"
  validator = true

  [[relaychain.nodes]]
  name = "bob"
  validator = true

[[parachains]]
id = 2000
cumulus_based = true
chain = "staking-rococo-dev"

  # run charlie as parachain collator
  [[parachains.collators]]
  name = "charlie-collator"
  validator = true
  command = "./target/release/polkadot-parachain"
  args = ["--force-authoring"]
