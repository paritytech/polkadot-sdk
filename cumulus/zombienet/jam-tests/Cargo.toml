[package]
name = "cumulus-jam-zombienet-tests"
version = "0.1.0"
description = "Zombienet-sdk tests for the Cumulus JAM collator."
authors.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
anyhow = { workspace = true }
array-bytes = { workspace = true }
chrono = { workspace = true }
codec = { workspace = true, default-features = true }
env_logger = { workspace = true }
log = { workspace = true }
serde_json = { workspace = true, default-features = true }
tokio = { workspace = true, features = ["macros", "process", "rt-multi-thread", "signal", "time"] }
jsonrpsee = { workspace = true, features = ["ws-client"] }
sp-core = { workspace = true, default-features = true }
sp-keyring = { workspace = true, default-features = true }
sp-runtime = { workspace = true, default-features = true }
tempfile = { workspace = true }
# The authorizer hash written into genesis has to be the one the collator derives, so it is built
# out of the very crates the collator and the guest use — the same path deps polkadot-omni-node-lib
# names.
jam-cumulus-facade = { package = "cumulus", path = "../../../../parachain-service/cumulus" }
jam-std-common = { path = "../../../../parachain-service/vendor/polkajam/crates/jam-std-common" }
# The byte strings the JAM RPC carries are base64, and this is where that spelling is defined.
jam-types = { path = "../../../../parachain-service/vendor/polkajam/crates/jam-types", features = [
	"bytes",
	"serde",
	"std",
] }
# TEMPORARY: points at a local zombienet-sdk checkout carrying the jam genesis net_addr
# fix. Restore the git rev below once that fix is merged into PR #573.
#   zombienet-sdk = { git = "https://github.com/paritytech/zombienet-sdk", rev = "74a1d56abdd1b4b451afca26b77d34e88bc58918" }
zombienet-sdk = { path = "../../../../zombienet-sdk/crates/sdk" }

[features]
jam-ci = []
