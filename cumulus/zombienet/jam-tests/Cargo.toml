[package]
name = "cumulus-jam-zombienet-tests"
version = "0.1.0"
description = "Zombienet-sdk tests for the Cumulus JAM collator."
authors.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
anyhow = { workspace = true }
array-bytes = { workspace = true }
chrono = { workspace = true }
codec = { workspace = true, default-features = true }
env_logger = { workspace = true }
log = { workspace = true }
serde_json = { workspace = true, default-features = true, features = ["raw_value"] }
tokio = { workspace = true, features = ["macros", "process", "rt-multi-thread", "signal", "time"] }
jsonrpsee = { workspace = true, features = ["ws-client"] }
# The proxy speaks polkajam's jsonrpsee 0.26 surface; the harness keeps 0.24 for `JamRpc`.
jsonrpsee-jam = { workspace = true, features = ["server", "ws-client"] }
cumulus-primitives-core = { workspace = true, default-features = true }
sp-core = { workspace = true, default-features = true }
sp-keyring = { workspace = true, default-features = true }
sp-runtime = { workspace = true, default-features = true }
tempfile = { workspace = true }
# The authorizer hash written into genesis has to be the one the collator derives, so it is built
# out of the very crates the collator and the guest use — the same path deps polkadot-omni-node-lib
# names. `parachain-service-core` is the consolidated types/keys/authorizer crate the former
# `cumulus` facade re-exported from.
parachain-service-core = { path = "../../../../parachain-service/core", features = ["std"] }
parachain-authorizer = { path = "../../../../parachain-service/authorizer", features = ["std"] }
parachain-authorizer-ed25519 = { path = "../../../../parachain-service/authorizer-ed25519" }
parachain-authorizer-sr25519 = { path = "../../../../parachain-service/authorizer-sr25519" }
# The genesis `gen-spec` compiles is built by `parachain-chain-spec` (uncommitted sibling-repo
# work), so the storage layout and authorizer hashes cannot drift from what the service decodes.
parachain-chain-spec = { path = "../../../../parachain-service/chain-spec" }
jam-std-common = { path = "../../../../parachain-service/vendor/polkajam/crates/jam-std-common" }
# The byte strings the JAM RPC carries are base64, and this is where that spelling is defined.
jam-types = { path = "../../../../parachain-service/vendor/polkajam/crates/jam-types", features = [
	"bytes",
	"serde",
	"std",
] }
zombienet-sdk = { workspace = true }

[features]
jam-ci = []
