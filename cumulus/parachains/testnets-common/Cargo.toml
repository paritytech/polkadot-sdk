[package]
name = "testnets-common"
version = "2.0.0"
authors.workspace = true
edition.workspace = true
description = "Logic and configuration specific to testnet parachain runtimes"
license = "Apache-2.0"

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
smallvec = "1.11.0"

# Substrate
frame-support = { path = "../../../substrate/frame/support", default-features = false, version = "27.0.0" }
sp-runtime = { path = "../../../substrate/primitives/runtime", default-features = false, version = "30.0.1" }

# Polkadot
rococo-runtime-constants = { path = "../../../polkadot/runtime/rococo/constants", default-features = false, version = "6.0.0" }
westend-runtime-constants = { path = "../../../polkadot/runtime/westend/constants", default-features = false, version = "6.0.0" }
polkadot-core-primitives = { path = "../../../polkadot/core-primitives", default-features = false, version = "6.0.0" }

# Cumulus

[dev-dependencies]

[build-dependencies]
substrate-wasm-builder = { version = "16.0.0", path = "../../../substrate/utils/wasm-builder" }

[features]
default = ["std"]
std = [
	"frame-support/std",
	"polkadot-core-primitives/std",
	"rococo-runtime-constants/std",
	"sp-runtime/std",
	"westend-runtime-constants/std",
]

runtime-benchmarks = [
	"frame-support/runtime-benchmarks",
	"sp-runtime/runtime-benchmarks",
]
