[package]
name = "pallet-bridge-parachains"
version = "0.13.0"
description = "Module that allows bridged relay chains to exchange information on their parachains' heads. (polkadot v1.12.0)"
authors.workspace = true
edition.workspace = true
license = "GPL-3.0-or-later WITH Classpath-exception-2.0"
repository.workspace = true

[lints]
workspace = true

[dependencies]
codec = { package = "parity-scale-codec", version = "3.6.1", default-features = false }
log = { workspace = true }
scale-info = { version = "2.11.1", default-features = false, features = ["derive"] }

# Bridge Dependencies

bp-header-chain = { path = "../../primitives/header-chain", default-features = false, version = "0.13.0" }
bp-parachains = { path = "../../primitives/parachains", default-features = false, version = "0.13.0" }
bp-polkadot-core = { path = "../../primitives/polkadot-core", default-features = false, version = "0.13.0" }
bp-runtime = { path = "../../primitives/runtime", default-features = false, version = "0.13.0" }
pallet-bridge-grandpa = { path = "../grandpa", default-features = false, version = "0.13.0" }

# Substrate Dependencies

frame-benchmarking = { path = "../../../substrate/frame/benchmarking", default-features = false, optional = true, version = "34.0.0" }
frame-support = { path = "../../../substrate/frame/support", default-features = false, version = "34.0.0" }
frame-system = { path = "../../../substrate/frame/system", default-features = false, version = "34.0.1" }
sp-runtime = { path = "../../../substrate/primitives/runtime", default-features = false, version = "37.0.0" }
sp-std = { path = "../../../substrate/primitives/std", default-features = false, version = "14.0.0" }
sp-trie = { path = "../../../substrate/primitives/trie", default-features = false, version = "35.0.0" }

[dev-dependencies]
bp-header-chain = { path = "../../primitives/header-chain" }
bp-test-utils = { path = "../../primitives/test-utils" }
sp-core = { path = "../../../substrate/primitives/core" }
sp-io = { path = "../../../substrate/primitives/io" }

[features]
default = ["std"]
std = [
	"bp-header-chain/std",
	"bp-parachains/std",
	"bp-polkadot-core/std",
	"bp-runtime/std",
	"codec/std",
	"frame-benchmarking/std",
	"frame-support/std",
	"frame-system/std",
	"log/std",
	"pallet-bridge-grandpa/std",
	"scale-info/std",
	"sp-runtime/std",
	"sp-std/std",
	"sp-trie/std",
]
runtime-benchmarks = [
	"frame-benchmarking/runtime-benchmarks",
	"frame-support/runtime-benchmarks",
	"frame-system/runtime-benchmarks",
	"pallet-bridge-grandpa/runtime-benchmarks",
	"sp-runtime/runtime-benchmarks",
]
try-runtime = [
	"frame-support/try-runtime",
	"frame-system/try-runtime",
	"pallet-bridge-grandpa/try-runtime",
	"sp-runtime/try-runtime",
]
