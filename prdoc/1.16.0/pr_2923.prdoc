title: "Use `console` crate instead of `ansi_term`"

doc:
  - audience: Node Dev
    description: |
      This PR replace obsoleted `ansi_term` to `console`.

crates:
  - name: relay-utils
    bump: patch
  - name: sc-informant
    bump: patch
  - name: sc-tracing
    bump: patch
  - name: sc-service
    bump: major
