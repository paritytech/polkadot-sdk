title: "collator-protocol: Remove race condition"

doc:
  - audience: Node Dev
    description: |
      Remove a race condition in the collator protocol that could lead
      to collations not being announced to a validator.

crates:
  - name: polkadot-collator-protocol
    bump: patch
