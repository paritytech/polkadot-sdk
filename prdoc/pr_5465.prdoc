title: try-state check invariant for nomination-pools (points >= stake)

doc:
  - audience: Runtime Dev
    description: |
      The Invariant holds the Each Bonded pool's point must never be lower than the stake in the pool

crates:
  - name: pallet-nomination-pools
    bump: minor
