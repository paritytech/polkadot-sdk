title: "[pallet_staking] Staking `force_unbond`"

doc:
  - audience: Runtime Dev
    description: |
      Force unbond when staking such that to fully unbond as a validator or nominator, chilled occurs first
      before unbonding

crates:
  - name: pallet-staking
