title: add fresh weights for westend asset hub
doc:
- audience: Todo
  description: |-
    - And deprecate `FastUnstake` as it is unsafe
    - And use the faster solver for benchmarks to speed things up
    - Remove hardcoded weights of westend.

    A step to reduce the diff size in https://github.com/paritytech/polkadot-sdk/pull/9925
crates:
- name: asset-hub-westend-runtime
  bump: patch
- name: pallet-election-provider-multi-block
  bump: patch
