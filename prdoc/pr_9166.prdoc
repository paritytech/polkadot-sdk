title: 'pallet-bags-list: Emit `ScoreUpdated` event only if it has changed'
doc:
- audience: Todo
  description: |-
    quick follow-up to https://github.com/paritytech/polkadot-sdk/pull/8684, ensuring all blocks don't have x events when the feature is enabled (as it is now in WAH)
crates:
- name: pallet-bags-list
  bump: patch
