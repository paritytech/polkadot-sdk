# Schema: Polkadot SDK PRDoc Schema (prdoc) v1.0.0
# See doc at https://raw.githubusercontent.com/paritytech/polkadot-sdk/master/prdoc/schema_user.json

title: nomination-pools config update should be AdminOrigin, not root

doc:
  - audience: "Runtime Dev"
    description: |
      Correctly sets the origin to tweak nomination pools configurations to a customizable
      `AdminOrigin`, not `ensure_root`.

crates:
  - name: pallet-nomination-pools
    bump: major
