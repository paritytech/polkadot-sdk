title: Replace `log` with `tracing` on `pallet-bridge-beefy`
doc:
- audience: Runtime Dev
  description: This PR replaces `log` with `tracing` instrumentation on `pallet-bridge-beefy`
    by providing structured logging.
crates:
- name: pallet-bridge-beefy
  bump: minor
