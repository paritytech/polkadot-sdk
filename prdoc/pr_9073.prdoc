title: Rate limit `DisputeRequest` sending in `dispute-distribution` to avoid overloading slow receivers
doc:
- audience: Node Dev
  description: |-
    Send `DisputeRequest` messages in batches to avoid overloading slow receivers and add a backoff
    when a peer rejects a request.

crates:
- name: polkadot-dispute-distribution
  bump: patch
- name: sc-network
  bump: minor
