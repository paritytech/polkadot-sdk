title: 'pallet-assets: allow force_create to set an arbitrary asset id when auto-increment is enabled'

doc:
- audience: Runtime Dev
  description: |-
    `force_create` no longer requires the asset id to equal `NextAssetId`: a `ForceOrigin` may
    create an asset with any id not currently in use, and when auto-increment is active the sequence
    is advanced past the chosen id so it can never later collide. Auto-increment is now configured
    explicitly via the new `Config::AssetIdSequencer` (`AutoIncAssetId` to enable, `()` to disable),
    replacing the implicit `NextAssetId`-is-`Some` plus `AutoIncAssetId` callback mechanism.

    Migration: every `pallet-assets` `Config` must set `AssetIdSequencer` — `AutoIncAssetId<Runtime, I>`
    where `CallbackHandle` previously used it (and drop it from `CallbackHandle`), otherwise `()`.

crates:
- name: pallet-assets
  bump: major
- name: asset-hub-rococo-runtime
  bump: patch
- name: asset-hub-westend-runtime
  bump: patch
- name: penpal-runtime
  bump: patch
- name: pallet-contracts-mock-network
  bump: patch
