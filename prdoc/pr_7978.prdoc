title: '`bp-runtime`: make macro expansion not rely on `sp-std` in scope, and move
  `paste` reexport to private module.'
doc:
- audience: Runtime Dev
  description: |-
    breaking change: bp-runtime no longer reexport `paste` in the root namespace, instead it is reexported in a module named `private`.
    update: macro in bp-runtime no longer require `sp-std` in scope.
crates:
- name: bp-runtime
  bump: major
