[relaychain]
default_command = "polkadot"
chain = "rococo-local"

[[relaychain.nodes]]
name = "alice"
validator = true
args = ["--network-backend=libp2p", "-lparachain=trace"]

[[relaychain.nodes]]
name = "bob"
validator = true
args = ["--network-backend=libp2p", "-lparachain=trace"]

[[parachains]]
id = 2000
cumulus_based = true
command="polkadot-omni-node"
chain_spec_runtime = { location = "../../target/release/wbuild/battleship-runtime/battleship_runtime.compact.wasm", preset = "local_testnet" }

# run charlie as parachain collator
[[parachains.collators]]
name = "charlie"
validator = true
image = "parity/polkadot-parachain:latest"
command = "polkadot-omni-node"
args = ["--network-backend=libp2p", "--ipfs-server", "--force-authoring", "--enable-statement-store", "--authoring=slot-based", "-lstatement-gossip=trace,statement-store=trace,runtime=trace,bitswap=trace,parachain=trace,aura=trace,txpool=trace"]

[[parachains.collators]]
name = "dave"
validator = false
image = "parity/polkadot-parachain:latest"
command = "polkadot-omni-node"
args = ["--network-backend=libp2p", "--ipfs-server", "-lstatement-gossip=trace,statement-store=trace,runtime=trace,bitswap=trace,txpool=trace", "--authoring=slot-based"]
